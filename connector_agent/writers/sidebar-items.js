initSidebarItems({"mod":[["dummy",""],["mixed",""]],"trait":[["PartitionWriter","`PartitionWriter` writes values to its own region. `PartitionWriter` is parameterized on lifetime `'a`, which is the lifetime of the parent `Writer`. This indicates the `PartitionWriter` can never live longer than the parent."],["Writer","A `Writer` is associated with a `TypeSystem` and a `PartitionWriter`. `PartitionWriter` allows multiple threads write data into the buffer owned by `Writer`."]]});